apiVersion: v1
kind: Service
metadata:
  name: profile-svc
  labels:
    app: profile-svc
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: profile-svc

