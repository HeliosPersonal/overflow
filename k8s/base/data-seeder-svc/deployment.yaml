apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-seeder-svc
  labels:
    app: data-seeder-svc
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-seeder-svc
  template:
    metadata:
      labels:
        app: data-seeder-svc
        component: worker
    spec:
      containers:
        - name: data-seeder-svc
          image: ghcr.io/placeholder/overflow-data-seeder:latest
          imagePullPolicy: Always
          env:
            - name: DOTNET_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: aspnetcore-environment
            # Service URLs (using internal k8s service names)
            - name: SeederOptions__QuestionServiceUrl
              value: "http://question-svc:8080"
            - name: SeederOptions__ProfileServiceUrl
              value: "http://profile-svc:8080"
            - name: SeederOptions__VoteServiceUrl
              value: "http://vote-svc:8080"
            # Keycloak Admin API credentials for user management
            - name: KeycloakOptions__AdminClientId
              valueFrom:
                secretKeyRef:
                  name: data-seeder-keycloak
                  key: admin-client-id
            - name: KeycloakOptions__AdminClientSecret
              valueFrom:
                secretKeyRef:
                  name: data-seeder-keycloak
                  key: admin-client-secret
            # Infisical credentials - if needed for other secrets
            - name: INFISICAL_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: infisical-credentials
                  key: INFISICAL_PROJECT_ID
                  optional: true
            - name: INFISICAL_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: infisical-credentials
                  key: INFISICAL_CLIENT_ID
                  optional: true
            - name: INFISICAL_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: infisical-credentials
                  key: INFISICAL_CLIENT_SECRET
                  optional: true
            # Disable file watching
            - name: DOTNET_USE_POLLING_FILE_WATCHER
              value: "true"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
