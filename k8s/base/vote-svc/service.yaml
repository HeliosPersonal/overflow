apiVersion: v1
kind: Service
metadata:
  name: vote-svc
  labels:
    app: vote-svc
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vote-svc

