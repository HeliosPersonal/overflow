apiVersion: v1
kind: Service
metadata:
  name: stats-svc
  labels:
    app: stats-svc
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: stats-svc

